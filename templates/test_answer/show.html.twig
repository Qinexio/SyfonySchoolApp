{% extends 'base.html.twig' %}

{% block title %}TestAnswer{% endblock %}

{% block body %}
    <h1>TestAnswer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ test_answer.id }}</td>
            </tr>
            <tr>
                <th>TestAnswerText</th>
                <td>{{ test_answer.testAnswerText }}</td>
            </tr>
            <tr>
                <th>TestAnswerIsCorrect</th>
                <td>{{ test_answer.testAnswerIsCorrect ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('test_answer_index') }}">back to list</a>

    <a href="{{ path('test_answer_edit', {'id': test_answer.id}) }}">edit</a>

    {{ include('test_answer/_delete_form.html.twig') }}
{% endblock %}
