{% extends 'base.html.twig' %}

{% block title %}UserDetail{% endblock %}

{% block body %}
    <h1>UserDetail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_detail.id }}</td>
            </tr>
            <tr>
                <th>UserEmail</th>
                <td>{{ user_detail.userEmail }}</td>
            </tr>
            <tr>
                <th>UserValid</th>
                <td>{{ user_detail.userValid ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>UserKey</th>
                <td>{{ user_detail.userKey }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_detail_index') }}">back to list</a>

    <a href="{{ path('user_detail_edit', {'id': user_detail.id}) }}">edit</a>

    {{ include('user_detail/_delete_form.html.twig') }}
{% endblock %}
